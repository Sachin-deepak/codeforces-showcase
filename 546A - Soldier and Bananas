#include <stdio.h>
long long int SUM(long long int k,long long int w)
{
  return w/2.0*(k+(k*w));
}
int main()
{
  long long int k,m,w,i,sum;
  scanf("%lld %lld %lld",&k,&m,&w);
  sum=SUM(k,w);
  i=sum-m;
  if (i<=0)
  {
      printf("0");
  }
  else 
    printf("%lld",i);
}
